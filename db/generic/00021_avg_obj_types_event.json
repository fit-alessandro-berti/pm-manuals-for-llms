{
  "name": "Avg Object Types per Event",
  "description": "Mean number of distinct object types linked to an event.",
  "code": "f = lambda ocel: ocel.relations.groupby('ocel:eid')['ocel:type'].nunique().reindex(ocel.events['ocel:eid'].unique(), fill_value=0).mean()"
}