{
  "name": "Avg Objects per Event",
  "description": "Average count of distinct objects linked to an event.",
  "code": "f = lambda ocel: ocel.relations.groupby('ocel:eid')['ocel:oid'].nunique().reindex(ocel.events['ocel:eid'].unique(), fill_value=0).mean()"
}
